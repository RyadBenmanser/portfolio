/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "brad-portfolio.netlify.app",
      "cdn.tuk.dev",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
